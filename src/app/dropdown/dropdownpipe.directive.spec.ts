import { DropdownpipeDirective } from './dropdownpipe.directive';

describe('DropdownpipeDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownpipeDirective();
    expect(directive).toBeTruthy();
  });
});
